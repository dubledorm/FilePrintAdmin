<%= form_with(model: printing_template, local: true) do |form| %>
  <% if printing_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(printing_template.errors.count, "error") %> prohibited this printing_template from being saved:</h2>

      <ul>
        <% printing_template.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :application_type %>
    <%= form.number_field :application_type %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
